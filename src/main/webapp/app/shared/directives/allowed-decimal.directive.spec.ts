import { AllowedDecimalDirective } from './allowed-decimal.directive';

describe('AllowedDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowedDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
